{
  "version": 2,
  "installCommand": "pip install -r requirements.txt && PLAYWRIGHT_BROWSERS_PATH=./pw-browsers python -m playwright install chromium",
  "builds": [
    {
      "src": "app.py",
      "use": "@vercel/python"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "app.py"
    }
  ]
}
